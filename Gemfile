source 'https://rubygems.org'

gemspec

# This isn't needed in gemspec because if somebody is requiring this gem
# in a Rubinius project, they'll already have to have this required.
platforms :rbx do
  gem 'rubysl', '~> 2.0'
end
